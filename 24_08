#include <stdio.h>
#include <stdlib.h>

// If (Se) usado para condições e else (Se não) para o inverso do if.
int main() {
 
 
	 	int a, b, c, resultado;
    
	printf("informe o valor de A, B, C: ");
	scanf("%d %d %d", &a, &b, &c);
	 

    if ( a>b ){
		resultado = a;     	
	
	} else{ 
	resultado = b;
	
   }
	
	if (c>resultado){
		resultado C;
	}    
    printf("%d", resultado);
 
 */
 
 
  int n, r;
  
   
  printf("Informe o numero: ");
  scanf("%d", &n);
  
  if(n>0){
  	r = n * -1;
  }else{
   r = n*n;
  }
  
  printf("%d", r);
   
  
  
	return 0;
}
